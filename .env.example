# SFTP Connection Configuration for Helios Pipeline
# ==================================================
# Copy this file to .env and fill in your credentials:
#   cp .env.example .env
#   chmod 600 .env
#
# IMPORTANT: .env is in .gitignore and will NOT be committed

# === Required Settings ===
SFTP_HOST="sftp.example.com"
SFTP_PORT=22
SFTP_USERNAME="your_username"

# === Authentication (choose ONE method) ===

# Option 1: Private Key (RECOMMENDED)
SFTP_PRIVATE_KEY_PATH="~/.ssh/id_rsa"
SFTP_PRIVATE_KEY_PASSPHRASE=""  # Optional, only if key is encrypted

# Option 2: Password
# SFTP_PASSWORD="your_password"

# === Notes ===
# - Use private key authentication when possible (more secure)
# - Supports RSA, Ed25519, ECDSA key formats
# - Files will be uploaded to /SCN_BDD/HELIOS/output/ (see metadata.yml)
# - See ../anais_staging/PRIVATE_KEY_SETUP_GUIDE.md for SSH key setup
